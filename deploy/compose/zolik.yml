# The production stack: one container, one image, no source on the host.
#
# This file is the entire deployment. It is copied to /home/zolik/compose.yml
# by scripts/deploy.sh alongside a .env, and the image it names arrives over
# SSH as a `docker load`. Nothing here builds anything — the host has no zolik
# source, no kdb checkout, no Go toolchain and no npm, and that is the point.
#
#   ZOLIK_RELEASE=1.1.1.41+2e01fae docker compose up -d
#
# Rolling back is the same line with an older release, as long as its image is
# still loaded (deploy.sh keeps the last few).
#
# The local development stack is server/docker-compose.kdb.yml, which builds
# from source and is otherwise the same shape.

# Stated, not inferred.
#
# Compose names a project after whatever directory the file happens to sit in,
# and prefixes volume names with it. This file lived under server/ until the
# deployment stopped shipping source, so the volume was called
# `server_kdb_data`; moving the file would have renamed the project to `zolik`,
# and Compose would have created a *new, empty* `zolik_kdb_data` without an
# error — a healthy container, a working sign-up page, and every account and
# match gone. Naming the project here means a future move cannot repeat it.
name: zolik

services:
  app:
    # Loaded by scripts/deploy.sh, never pulled and never built. The `:?`
    # refuses to start rather than silently resolving to `zolik:latest`, which
    # would be whichever image happened to be loaded last.
    image: zolik:${ZOLIK_RELEASE:?ZOLIK_RELEASE must name a loaded image tag}
    restart: unless-stopped
    # Sized for a 1–2 GB host: one Go binary with the web client embedded in
    # it, an embedded KDB, no Mongo and no Redis.
    mem_limit: 2g
    env_file:
      - .env
    # These override .env, so the container's shape is stated here rather than
    # depending on what a particular host's .env happens to hold. Every one is
    # a literal — no `${VAR:-false}` indirection on a production host, because
    # an unset variable somewhere in the environment should not be able to
    # open any of these.
    environment:
      FEATURE_FLAG_DB_ENGINE: kdb
      KDB_PATH: /data/kdb
      # KDB 0.4.0's history retention modes: the feature is on, and no mode is
      # named. Those are two different statements. An unset KDB_HISTORY_MODE
      # reads as "whatever this namespace already is", and every namespace on
      # this host predates the modes, so they keep every commit exactly as
      # they always have. Nothing here changes what is on disk.
      #
      # The empty mode is a literal for the reason the hatches above are: it
      # overrides .env, so what this host's namespaces keep cannot be decided
      # by a variable that turns up in an environment.
      #
      # Naming a mode IS a procedure, not an edit. A namespace refuses to open
      # under a mode it was not built with, so "none" here without first
      # dealing with what is in zolik_kdb_data switches nothing: the container
      # exits on open and Docker restarts it forever, which reads as a bad
      # build rather than a configuration change. The volume has to be
      # converted offline (kdb-inspect migrate-history) or wiped, accepting
      # the loss of every account, match and stat.
      #
      # Written down in docs/kdb-history-none-cutover.md. The mode line below
      # is the last step of it, not the first.
      FEATURE_FLAG_KDB_HISTORY_RETENTION: "true"
      KDB_HISTORY_MODE: ""
      PORT: "8090"
      # Off, and said out loud. Removing the SSH_* variables does not turn the
      # terminal client off — SSH_ENABLED defaults *on* whenever APP_ENV is
      # local, and its default host key path is one the image's nonroot user
      # cannot create. Left unstated, that combination crash-loops the
      # container: mkdir .ssh: permission denied, exit, restart, repeat.
      SSH_ENABLED: "false"
      # Belt and braces for the same reason: this also defaults on under a
      # local APP_ENV, and "any key is admitted" is not a default a public
      # host should ever be one edited line away from.
      SSH_ALLOW_ALL_KEYS: "false"
      # /matches/{id}/debug-state writes match state straight past the rules,
      # and /auth/dev/last-code hands back the sign-in code mailed to any
      # address. Both default ON under a local APP_ENV — which a production
      # env file may deliberately set, to keep guest play working without SMTP
      # — so they are closed here explicitly rather than by implication.
      ENABLE_TEST_ENDPOINTS: "false"
      # /debug/memory and /debug/pprof. A literal, not an interpolation: this
      # one can be asked to stop the world, and it reports the shape of the
      # process to anyone who can reach the port.
      ENABLE_DEBUG_ENDPOINTS: "false"
      # The operator console's port inside the container. It is a *second*
      # listener, and the ports: block below is what keeps it off the
      # internet — see the note there. ADMIN_USERNAME and ADMIN_PASSWORD_HASH
      # come from .env; without them the listener does not start at all.
      ADMIN_PORT: "8091"
      # 0.0.0.0 *inside the container*, which is not the loosening it looks
      # like. The ports: line below is what decides who can reach this, and it
      # publishes to the host's loopback only.
      #
      # The server defaults this to 127.0.0.1, which is right on a bare host
      # and silently fatal here: Docker forwards a published port to the
      # container's bridge address, so a console bound to the container's
      # loopback is reachable from nothing at all — the tunnel connects, gets
      # an empty reply, and the log still says the console is up. Two loopback
      # binds in series are not twice the boundary; they are a wall.
      ADMIN_BIND: "0.0.0.0"
      # Structured JSON to stdout, which is what `docker logs | jq` wants.
      LOG_LEVEL: info
    volumes:
      # /data is created nonroot-owned in the image, so the engine can write
      # under the distroless nonroot user.
      - kdb_data:/data
    ports:
      # The game. Loopback only — nginx on the host is the public entry point,
      # and it proxies everything here now that the server serves the web
      # client too.
      - "127.0.0.1:8090:8090"
      # The operator console, and the line that keeps it private.
      #
      # nginx does not proxy this port and has no vhost for it, so it is not
      # reachable from the internet at all — not guarded, not 403, absent. That
      # is deliberate: the public vhost is a single catch-all `location /`, so
      # anything the game's router registers is public by construction, and a
      # console mounted there would be one guard bug away from the world. The
      # server therefore registers the console on a different router entirely
      # (see internal/app/admin.go), and this maps it to the *host's* loopback
      # only.
      #
      # An operator reaches it by tunnelling, with the key that already
      # deploys this:
      #
      #   ssh -N -L 8091:127.0.0.1:8091 davja@192.168.13.13
      #   open http://127.0.0.1:8091/admin
      #
      # Do not change this to "8091:8091". That binds every interface on the
      # host, which publishes the console to the LAN and, through the WAN NAT
      # that already forwards to this box, potentially further.
      - "127.0.0.1:8091:8091"

volumes:
  kdb_data:
    # The database's identity, pinned so it survives this file being moved,
    # renamed, or run from a different directory. See the note on `name:` above
    # — this is the line that makes "the data outlives the deploy" true.
    name: zolik_kdb_data
